package edu.duke.ece651.risc.shared;

import static org.junit.jupiter.api.Assertions.*;

import java.util.*;

import org.junit.jupiter.api.Test;

public class MapViewTest {
  @Test
  public void test_display() {
    /*
    List<String> playerNames=new ArrayList<>();
    playerNames.add("John");
    playerNames.add("Tom");
    GameMap map=new GameMap(playerNames,1);

    for(String playerName:map.getAllPlayerNames()){
      for(Territory t:map.getPlayerTerritories(playerName)){
        t.setMyArmy(new BasicArmy(playerName,1));
      }
    }

    MapView view=new MapView(map);

    String actual=view.display();

    String expected="John player:\n-------------\n1 units in 0(next to: )\n\nTom player:\n-------------\n1 units in 1(next to: )\n\n";
    assertEquals(actual,expected);
*/
  }

}
